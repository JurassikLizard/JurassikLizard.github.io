---
title: Blog
recent_post_count: 3
layout: main
---

<h2>Recent Posts</h2>

{% for post in collections.post reversed %}
  {% if recent_post_count > 0 %}
    {% assign recent_post_count = recent_post_count | minus: 1 %}
    <p>
      <a href="{{post.url}}">{{ post.data.title }}</a>
      -
      {{ post.date | niceDate }}<br/>
      {{ post.data.preview }}
    </p>
  {% endif %}
{% endfor %}